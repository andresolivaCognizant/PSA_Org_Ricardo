({
	// recordUpdated : function( component, event, helper ){
	// 	helper.handleSaveRecord( component, event, helper );
	// },
	recordSubmit: function (component, event, helper){
		helper.handleSubmitRecord( component, event,helper);
	}
})