({

    doInit : function (component, event, helper){
        helper.executeInit(component, event, helper);
    }, 

    doSave : function (component, event, helper){
        helper.executeSave(component, event, helper);
    }
})